<form method="post" action="{{ path('app_restaurant_delete', {'id': restaurant.id}) }}" onsubmit="return confirm('Voulez vous vraiment sur ce restaurant?');" class="form_suppri">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ restaurant.id) }}">
    <button class="btn_suppr_resto">Supprimer</button>
</form>
